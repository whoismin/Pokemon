import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    width: '100%',
  },

  button: {
    width: '100%',
    height: 62,
    borderRadius: 16,
    marginTop: 35,
    marginBottom: 20,
    alignItems: 'center',
    justifyContent: 'center',
    elevation: 6,

  },

  buttonText: {
    color: '#FFF',
    fontSize: 20,
    fontWeight: '900',
  },
});